{
  "name": "linkwarden-obsidian-sync-tooling",
  "private": true,
  "description": "Not a JavaScript project. This exists so commitlint can resolve the shared config it extends, which bunx alone cannot do, and so lefthook is pinned like everything else.",
  "packageManager": "bun@1.3.14",
  "devDependencies": {
    "@commitlint/cli": "21.2.2",
    "@commitlint/config-conventional": "21.2.2",
    "lefthook": "2.1.12"
  },
  "trustedDependencies": ["lefthook"],
  "scripts": {
    "prepare": "lefthook install"
  }
}
